# src/main/resources/db/changelog/changes/0000__bootstrap.yml
databaseChangeLog:
# Celowo brak changeSetów — plik tylko „odblokowuje” includeAll.
# Pierwsze właściwe migracje dorzucisz jako kolejne pliki w tym katalogu
