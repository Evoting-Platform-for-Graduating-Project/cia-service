openapi: 3.0.3
info:
  title: Evoting API
  version: "1.0.0"
servers:
  - url: /api
paths:
  /health:
    get:
      tags: [Health]
      operationId: healthCheck
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: UP

